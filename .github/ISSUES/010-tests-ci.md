Title: Add tests and CI for the API

Description:
Add unit and integration tests for API endpoints and set up GitHub Actions to run tests on push/PR.

Acceptance criteria:
- Test suite covering key endpoints.
- GitHub Actions workflow that runs tests and reports results.

Labels: test, ci
