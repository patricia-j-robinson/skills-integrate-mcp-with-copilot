Title: Add user authentication and accounts

Description:
Add student accounts and authentication so signups are tied to authenticated users instead of raw emails. Support basic email/password and optionally OAuth.

Acceptance criteria:
- User model and registration/login endpoints implemented.
- Endpoints require authentication for signup/unregister.
- Frontend updated to allow login and show user's signups.

Labels: enhancement, security
